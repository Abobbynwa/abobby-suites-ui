services:
  # Frontend service - React app
  - type: web
    name: abobby-suites-frontend
    env: static
    buildCommand: "npm install && npm run build"
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://technocrat:8001
